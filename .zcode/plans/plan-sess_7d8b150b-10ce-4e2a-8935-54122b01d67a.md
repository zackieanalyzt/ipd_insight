## แผนลบ: ตารางสรุปผู้ป่วยนอก + กราฟแท่ง amphur

### การเปลี่ยนแปลงในแต่ละไฟล์

#### 1. `index.html`
- **บรรทัด 971-982:** ลบ `<div class="visual-container">` ของ Geo Chart (canvas id `opdGeoChart`)
- **บรรทัด 985-1021:** ลบ `<div class="details-container">` ของตาราง (table id `opd-data-table`, search input `opd-table-search`, export button `btn-opd-export-csv`, table-info)

#### 2. `app.js`
- **บรรทัด ~58:** ลบ `let opdGeoChartInstance = null;`
- **บรรทัด ~58-60:** ลบตัวแปร `opdSortColumn`, `opdSortDirection`, `opdTableSearchQuery`
- **บรรทัด 159-161:** ลบ `opdTableSearchInput`, `opdTableBody`, `btnOpdExportCsv` DOM references
- **บรรทัด 558-559:** ลบ `opdTableSearchInput.value = ''` และ `opdTableSearchQuery = ''`
- **บรรทัด 3000:** ลบ `renderOpdGeoChart(filtered);`
- **บรรทัด 3003:** ลบ `renderOpdTable(filtered);`
- **บรรทัด 3152-3188:** ลบฟังก์ชัน `renderOpdGeoChart()` ทั้งก้อน
- **บรรทัด 3190-3293:** ลบฟังก์ชัน `renderOpdTable()` ทั้งก้อน
- **บรรทัด 3295-3339:** ลบฟังก์ชัน `exportOpdTableToCsv()` ทั้งก้อน
- **บรรทัด 3357-3392:** ลบ event listeners ของ search input, export button, table header sort

#### 3. `index.css`
- **ไม่ต้องแก้** — CSS ของตารางเป็น shared style ที่ใช้ร่วมกับ Dashboard อื่น

#### 4. `server.js` / `config.json`
- **ไม่ต้องแก้** — ฝั่ง server ส่งข้อมูล OPD ตามเดิม ตัวกรอง amphur ยังใช้งานได้

### คงไว้:
- ตัวกรองอำเภอ (`selectedOpdAmphur`, `opdAmphurSelect`)
- แผนภูมิอื่นๆ (Trend, Diag Type, Insurance) — ยังกรองตาม amphur ได้
- ข้อมูล OPD ทั้งหมดใน server