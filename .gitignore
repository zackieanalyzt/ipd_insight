node_modules/
.DS_Store

# ── Secrets (ห้าม commit) ──────────────────────────────
config.json
.env
.env.*
!.env.example

# ── ข้อมูลผู้ป่วย / ข้อมูลภายใน (ห้าม commit) ──────────
*.csv
*/*.csv
**/*.csv
*.xlsx
*.gz
*.log

# ── tooling / agent state ─────────────────────────────
.zcode/
.thclaws/
tmp/

# ── รายงานความปลอดภัย (ไม่ควรอัปโหลดสาธารณะ) ──────────
SECURITY-AUDIT.md
SUMMARY-PHASE1.md

# ── Auth (local dev db) ────────────────────────────────
auth.db
auth.db-*
