node_modules
npm-debug.log

# ── secrets — injected via docker-compose bind mount, never baked into the image ──
config.json
.env
.env.*

# ── VCS / tooling ──
.git
.gitignore
.zcode
.thclaws
Dockerfile
docker-compose.yml
tmp

# ── logs / reports / OS noise ──
*.log
*.xlsx
.DS_Store
*.md
